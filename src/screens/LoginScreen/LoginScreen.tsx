import {View} from 'react-native';
import colors from '../../lib/colors';

const LoginScreen = () => {
  return <View style={{flex: 1, backgroundColor: colors.gray.light}} />;
};

export default LoginScreen;
